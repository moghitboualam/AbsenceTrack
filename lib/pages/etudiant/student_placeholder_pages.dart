import 'package:flutter/material.dart';

class MesAbsencesPage extends StatelessWidget {
  const MesAbsencesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Page Mes Absences (En construction)"),
      ),
    );
  }
}

class MesSeancesPage extends StatelessWidget {
  const MesSeancesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Page Mes Séances (En construction)"),
      ),
    );
  }
}

class MesJustificationsPage extends StatelessWidget {
  const MesJustificationsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Page Mes Justifications (En construction)"),
      ),
    );
  }
}
