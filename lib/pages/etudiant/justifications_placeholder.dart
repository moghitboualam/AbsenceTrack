import 'package:flutter/material.dart';

class MesJustificationsPage extends StatelessWidget {
  const MesJustificationsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Justifications")),
      body: const Center(child: Text("Page Justifications (À implémenter)")),
    );
  }
}
